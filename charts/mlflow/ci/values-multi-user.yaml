# CI values for Kubeflow multi-user deployment testing.

mlflow:
  backendStoreUri: "sqlite:////mlflow/mlflow.db"
  artifactsDestination: "file:///mlflow/artifacts"
  authorizationMode: subject_access_review
  staticPrefix: /mlflow
  port: 5000

env:
  - name: MLFLOW_K8S_AUTH_REMOTE_USER_HEADER
    value: x-auth-request-email
  - name: MLFLOW_K8S_AUTH_REMOTE_GROUPS_HEADER
    value: x-auth-request-groups

service:
  port: 5000

storage:
  enabled: true
  size: 2Gi

tls:
  enabled: false

istio:
  enabled: true
  virtualService:
    enabled: true
    gateways:
      - kubeflow-gateway
    hosts:
      - "*"
  authorizationPolicy:
    enabled: true
    action: ALLOW
    rules:
      - from:
          - source:
              principals:
                - "cluster.local/ns/istio-system/sa/istio-ingressgateway-service-account"
  destinationRule:
    enabled: true
    trafficPolicy:
      tls:
        mode: ISTIO_MUTUAL

networkPolicy:
  enabled: true

metrics:
  enabled: false

garbageCollection:
  enabled: false

traceArchival:
  enabled: false
